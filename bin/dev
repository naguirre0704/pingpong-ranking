#!/usr/bin/env sh

# Runs Rails (port 3000) and the Vite dev server together via foreman.
if ! gem list --silent --installed foreman; then
  echo "Instalando foreman..."
  gem install foreman
fi

exec foreman start -f Procfile.dev "$@"
